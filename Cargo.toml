[package]
name = "cache-data-operation"
version = "0.1.0"
edition = "2024"

[dependencies]
redis = "0.24"
tokio = { version = "1.0", features = ["full"] }
clap = { version = "4.0", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
mockall = "0.11"
redis-test = "0.1"
